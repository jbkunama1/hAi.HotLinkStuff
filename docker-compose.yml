version: '3.8'

services:
  hotlinkstuff:
    image: ghcr.io/jbkunama1/hai.hotlinkstuff:latest
    container_name: hai-hotlinkstuff
    ports:
      - "8080:80"
    volumes:
      - hotlinkstuff_data:/var/www/html/data
    restart: unless-stopped

volumes:
  hotlinkstuff_data:
