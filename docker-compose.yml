version: '3.8'

services:
  app:
    image: ghcr.io/jbkunama1/hai.hotlinkstuff:latest
    container_name: hai-hotlinkstuff
    ports:
      - "8080:80"
    volumes:
      - data:/var/www/html/data
    restart: unless-stopped

volumes:
  data:
